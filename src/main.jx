import React from "react";
import ReactDOM from "react-dom/client";
import MarketSignalBoard from "./MarketSignalBoard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MarketSignalBoard />
  </React.StrictMode>
);
